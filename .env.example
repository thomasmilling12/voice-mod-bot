# Copy this file to .env and fill in your values
# The bot reads DISCORD_BOT_TOKEN from the environment (not from .env directly).
# On the Pi, set it via the systemd service file or export it in your shell.

DISCORD_BOT_TOKEN=your_bot_token_here
